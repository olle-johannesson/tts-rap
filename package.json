{
  "name": "rap",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "rimraf dist && rimraf .parcel-cache && parcel build index.html --no-source-maps"
  },
  "keywords": [],
  "author": "Olle Johannesson",
  "license": "ISC",
  "dependencies": {
    "easy-speech": "^1.2.0",
    "parcel": "^2.8.0",
    "syllable": "^5.0.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
