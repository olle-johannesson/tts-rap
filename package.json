{
  "name": "rap",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "clean": "rimraf dist && rimraf public && rimraf .parcel-cache",
    "build": "npm run-script clean && parcel build src/index.html --no-source-maps"
  },
  "keywords": [],
  "author": "Olle Johannesson",
  "license": "ISC",
  "dependencies": {
    "easy-speech": "^1.2.0",
    "syllable": "^5.0.1"
  },
  "devDependencies": {
    "parcel": "^2.8.0",
    "rimraf": "^3.0.2"
  }
}
